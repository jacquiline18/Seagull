{
  "name": "seagull-general-supply",
  "version": "1.0.0",
  "description": "Seagull General Supply Limited - Full-Stack Laboratory Equipment Portal",
  "scripts": {
    "build": "npm --prefix server install && npm --prefix client install && npm --prefix client run build",
    "install:all": "npm --prefix server install && npm --prefix client install",
    "dev:client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server run dev",
    "build:client": "npm --prefix client run build",
    "build:server": "npm --prefix server install",
    "start": "npm --prefix server start"
  }
}
